{
  "_comment": "Cursor project MCP config. Copy to .cursor/mcp.json (gitignored) and replace the password. Equivalent command: python -m graphforge mcp",
  "mcpServers": {
    "graphforge": {
      "command": "graphforge",
      "args": ["mcp"],
      "env": {
        "NEO4J_URI": "bolt://127.0.0.1:7687",
        "NEO4J_USER": "neo4j",
        "NEO4J_PASSWORD": "please-change-me",
        "NEO4J_DATABASE": "neo4j"
      }
    }
  }
}
