{
  "_comment": "You almost certainly do not need this file. Run `graphforge mcp install` and it writes the entry for you, into the right per-OS path, with the console script resolved from the interpreter graphforge is actually installed into. Use this only to merge by hand. Note there is no password here: --env points at your .env so the secret stays in one place. Replace the placeholder paths with absolute ones, then restart the client.",
  "mcpServers": {
    "graphforge": {
      "command": "/absolute/path/to/graphforge",
      "args": ["mcp", "--env", "/absolute/path/to/.env"]
    }
  }
}
