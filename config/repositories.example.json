{
  "_comment": "Copy to config/repositories.json (gitignored) and edit. `graphforge git -c config/repositories.json` reads this. You can also skip the file entirely and pass repos positionally: `graphforge git <git-url-or-local-path> --name my-service`.",
  "defaultBranch": "main",
  "repositories": [
    {
      "name": "example-service",
      "url": "https://github.com/octocat/Hello-World.git",
      "branch": "master"
    },
    {
      "name": "local-checkout",
      "path": "/absolute/path/to/an/already/cloned/repo"
    }
  ]
}
