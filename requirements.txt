# This file is not the source of truth. pyproject.toml is.
#
# It duplicated the dependency list, with nothing enforcing that the two agreed
# -- so it could drift silently, and did not survive the move to optional
# database drivers. Install from the project instead:
#
#   pip install graphforge-neo4j                 # core: git ingest, MCP-less
#   pip install 'graphforge-neo4j[mcp]'          # + the MCP server
#   pip install 'graphforge-neo4j[postgres]'     # + PostgreSQL   (mysql, mssql too)
#   pip install 'graphforge-neo4j[gitlab]'       # + GitLab group auto-discovery
#   pip install 'graphforge-neo4j[all]'          # everything
#
# From a source checkout:
#
#   pip install -e '.[dev,all]'
#
-e .
