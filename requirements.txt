# graphforge-neo4j runtime dependencies.
# Easiest install:  pip install graphforge-neo4j
# From source:      pip install -e .

neo4j>=5.0.0
python-dotenv>=1.0.0
tqdm>=4.65.0
requests>=2.28.0

# Database drivers (bundled by default so any source works out of the box).
mysql-connector-python>=8.0
psycopg2-binary>=2.9
pyodbc>=5.0            # SQL Server; also needs a system ODBC driver at run time

# Optional: the MCP server (expose the graph to Claude / any MCP client)
#   pip install "graphforge-neo4j[mcp]"
